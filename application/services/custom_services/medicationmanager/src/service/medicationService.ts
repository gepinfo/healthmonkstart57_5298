
import { CustomLogger } from '../config/Logger';
import * as jwt from 'jsonwebtoken';


export class medicationService {
    
    constructor() { }
    
    public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into medicationService.ts: ')
     let  foodData = req.body;
     medication.(foodData,(response)=>{
             new CustomLogger().showLogger('info', 'Exit from medicationService.ts: ')
         callback(response);
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into medicationService.ts: ')
     let  foodData = req.body;
     medication.(foodData,(response)=>{
             new CustomLogger().showLogger('info', 'Exit from medicationService.ts: ')
         callback(response);
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into medicationService.ts: ')
     let  foodData = req.body;
     medication.(foodData,(response)=>{
             new CustomLogger().showLogger('info', 'Exit from medicationService.ts: ')
         callback(response);
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into medicationService.ts: ')
     let  foodData = req.body;
     medication.(foodData,(response)=>{
             new CustomLogger().showLogger('info', 'Exit from medicationService.ts: ')
         callback(response);
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into medicationService.ts: ')
     let  foodData = req.body;
     medication.(foodData,(response)=>{
             new CustomLogger().showLogger('info', 'Exit from medicationService.ts: ')
         callback(response);
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into medicationService.ts: ')
     let  foodData = req.body;
     medication.(foodData,(response)=>{
             new CustomLogger().showLogger('info', 'Exit from medicationService.ts: ')
         callback(response);
         });
    }
    
public  (req: Request, callback){
    new CustomLogger().showLogger('info', 'Enter into medicationService.ts: ')
     let  foodData = req.body;
     medication.(foodData,(response)=>{
             new CustomLogger().showLogger('info', 'Exit from medicationService.ts: ')
         callback(response);
         });
    }
    
    
    
    
}